import styled from 'styled-components/native';
import { theme } from '~constants/theme';

export const Text = styled.Text`
    font-family: 'Montserrat-Regular';
    font-size: 12px;
    line-height: 15px;
    color: ${theme.INFO_TEXT_COLOR};
`;
